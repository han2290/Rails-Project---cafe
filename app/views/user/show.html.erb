<h1><%= @user.name%></h1>
최종 접속 아이피: <p><%=@user.ip_address%></p>
<hr>
<ul class= "list-group">
    <% @user.posts.each do |post|%>
        <%= link_to post.title, "/board/#{post.id}", class: "list-group-item" %>
    <% end %>
</ul>
<hr>

<%= link_to "목록", "/users"%>
<%= link_to "수정", "/user/#{@user.id}/edit"%>